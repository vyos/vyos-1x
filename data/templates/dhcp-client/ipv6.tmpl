# generated by dhcp.py
interface {{ ifname }} {
	request domain-name-servers,domain-name;
{%  if dhcpv6_prm_only %}
    information-only;
{%  endif %}
{%  if not dhcpv6_temporary %}
    send ia-na 0;
{%  endif %}
};
