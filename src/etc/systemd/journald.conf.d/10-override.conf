[Journal]
# rsyslog reads the journal directly through imjournal, so forwarding the same
# entries to /dev/log delivers every non-kernel message twice: once via
# imuxsock and once via imjournal. Local files under /var/log and every remote
# syslog target get both copies (T9183).
ForwardToSyslog=no
MaxLevelSyslog=debug
