
name: "PR Conflicts checker"
on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  pull-requests: write
  contents: read

jobs:
  check-pr-conflict-call:
    uses: vyos/.github/.github/workflows/check-pr-conflict.yml@T7748-conflict-check-fix
    secrets: inherit
