
name: "PR Conflicts checker"
on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  pull-requests: write
  contents: read

jobs:
  check-pr-conflict-call:
    uses: vyos/.github/.github/workflows/check-pr-conflict.yml@T7848-disable-conflict-check-workflow-current
    secrets: inherit
