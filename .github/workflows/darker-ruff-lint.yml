name: Python Lint (Darker + Ruff)
on:
  pull_request:
    branches:
      - current

permissions:
  pull-requests: write
  contents: read

jobs:
  darker-ruff-lint:
    uses: vyos/.github/.github/workflows/lint-with-darker-ruff.yml@T7592-black-quote-ruff-fix
    secrets: inherit
